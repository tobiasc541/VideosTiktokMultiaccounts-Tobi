type Row={userId:string;email:string;name:string;plan:string;requests:number;input:number;cached:number;output:number;cost:number};
type Official={cost:number;currency:string;available:boolean;error?:string};
export default function AdminAiCosts({rows,mrr,official}:{rows:Row[];mrr:number;official:Official}){const requests=rows.reduce((s,r)=>s+r.requests,0),attributed=rows.reduce((s,r)=>s+r.cost,0),tokens=rows.reduce((s,r)=>s+r.input+r.output,0),realCost=official.available?official.cost:attributed,margin=mrr-realCost;return <section className="adminSection" id="ai-costs"><div className="adminSectionHead"><div><small>OPENAI · COST CONTROL</small><h2>Consumo de inteligencia artificial</h2></div><span>{official.available?"Gasto oficial OpenAI · mes actual":"Tracking interno VYRAL · mes actual"}</span></div><div className="adminGrowthGrid"><article><small>GASTO OFICIAL OPENAI</small><strong>US$ {realCost.toFixed(4)}</strong><span>{official.available?"Leído directamente de OpenAI":"Sin conexión al Costs API; mostrando atribuido"}</span></article><article><small>ATRIBUIDO A USUARIOS</small><strong>US$ {attributed.toFixed(4)}</strong><span>{requests} usos · {tokens.toLocaleString("es-AR")} tokens</span></article><article><small>PROMEDIO / USUARIO</small><strong>US$ {(rows.length?attributed/rows.length:0).toFixed(4)}</strong><span>{rows.length} usuarios con consumo atribuido</span></article><article><small>MRR − IA OFICIAL</small><strong>US$ {margin.toFixed(2)}</strong><span>Antes de otros costos y comisiones</span></article></div>{!official.available&&<div className="adminEmpty" style={{marginTop:12}}>No pudimos leer todavía el gasto oficial de OpenAI. Revisá que OPENAI_ADMIN_KEY esté en Production y que el deployment nuevo la tenga disponible.</div>}{!rows.length?<div className="adminEmpty" style={{marginTop:12}}>Aún no hay consumo atribuido por usuario desde que activamos el tracking. El gasto oficial de arriba puede incluir consumo anterior.</div>:<div className="adminAiTable"><div className="adminAiHead"><span>Usuario</span><span>Plan</span><span>Usos</span><span>Tokens</span><span>Costo USD</span></div>{rows.map(r=><div className="adminAiRow" key={r.userId}><div><strong>{r.name}</strong><small>{r.email}</small></div><span>{r.plan||"Sin plan"}</span><strong>{r.requests}</strong><span>{(r.input+r.output).toLocaleString("es-AR")}</span><strong>US$ {r.cost.toFixed(5)}</strong></div>)}</div>}<p className="adminAiNote">El gasto oficial se consulta con la Admin API de OpenAI. El desglose por usuario proviene del tracking interno de VYRAL, por eso puede diferir si hubo consumo anterior o externo a VYRAL.</p></section>}
