import {NextResponse} from "next/server";import {metaState} from "../../../../../lib/meta-instagram";import {env} from "../../../../../lib/env";import {socialAccountContext} from "../../../../../lib/social-account-limits";
export async function GET(req:Request){const c=await socialAccountContext();if(!c)return NextResponse.redirect(new URL("/login",req.url));if(c.total>=c.limit)return NextResponse.redirect(new URL("/?account_limit=1",req.url));const redirect=(process.env.APP_URL||"https://vyralvideos.com").replace(/\/$/,"")+"/api/meta/facebook/callback";const u=new URL("https://www.facebook.com/"+(process.env.META_GRAPH_API_VERSION||"v24.0")+"/dialog/oauth");u.searchParams.set("client_id",env("META_INSTAGRAM_APP_ID"));u.searchParams.set("redirect_uri",redirect);u.searchParams.set("state",metaState(c.session.userId));u.searchParams.set("scope","pages_show_list,pages_read_engagement,pages_manage_posts,pages_manage_metadata,pages_messaging,business_management");return NextResponse.redirect(u);}